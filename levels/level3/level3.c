#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char* idk = "/bin/sh";

void solve(char* a)
{
    system(a);
}
int main()
{
    char buf[100];
    printf("LOL.....Isn't this just level 2 again???!!!?!!?!!?!\n\n");
    read(0, buf, 200);   
}